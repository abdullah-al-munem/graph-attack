conda create --name gnn_attack python=3.10 -y && conda activate gnn_attack && pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124 && pip install deeprobust pandas && export TORCH=$(python -c "import torch; print(torch.__version__)") && pip install -q torch-scatter -f https://data.pyg.org/whl/torch-${TORCH}.html && pip install -q torch-sparse -f https://data.pyg.org/whl/torch-${TORCH}.html && pip install -q git+https://github.com/pyg-team/pytorch_geometric.git && pip install ogb
